{
  "name": "weerter-machines",
  "version": "1.0.0",
  "description": "De Weerter Machines - Triathlon Training App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "concurrently \"node server.js\" \"cd client && npm run dev\"",
    "seed": "node seed.js",
    "build": "cd client && npm install && npm run build",
    "setup": "npm install && cd client && npm install"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^9.4.3",
    "concurrently": "^8.2.2",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "multer": "^1.4.5-lts.1"
  }
}
